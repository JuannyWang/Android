<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ghost.patrol"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" >
    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ghost.patrol.StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".TaskTableActivity" />
        <activity android:name=".SetEnvironmentActivity" />
        <activity android:name=".DisplayUserActivity" />
        <activity android:name=".ChangeUserActivity" />
        <activity android:name=".LogViewActivity" />
        <activity android:name=".GPSLocationActivity" />
        <activity android:name=".TourItemActivity" />
        <activity android:name=".CreateViewActivity" />
        <activity android:name=".DefectDetailActivity" />
        <activity android:name=".NoticeActivity" />
    </application>

</manifest>